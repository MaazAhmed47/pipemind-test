```json
{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    // Your project dependencies here, e.g.,
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  }
}
```
Please replace `"your-project-name"`, `"1.0.0"`, and the dependencies with your actual project details. This example assumes a Node.js project using npm or yarn for package management. 

If your project uses a different package manager (like pnpm) or has different requirements, adjust accordingly. 

This is a basic example to get you started. The actual content should reflect your project's specific needs and current state. 

Make sure to adjust versions and add actual dependencies as per your project's requirements.